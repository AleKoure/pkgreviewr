% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_package_review_report.R
\name{build_report}
\alias{build_report}
\title{Build a package review report.}
\usage{
build_report(
  package_url,
  output_path = "test_review.md",
  chat_fn = NULL,
  chat = NULL,
  parallel = FALSE,
  workers = 1L,
  artifact_dir = NULL
)
}
\arguments{
\item{package_url}{Repository URL to review.}

\item{output_path}{Output path for the generated markdown report.}

\item{chat_fn}{Optional chat function with signature
\verb{(system_prompt, user_prompt)}.}

\item{chat}{Optional \code{ellmer} chat object inheriting from \code{Chat}.}

\item{parallel}{Whether to generate independent review sections in parallel
when possible.}

\item{workers}{Number of workers to use when \code{parallel = TRUE}.}

\item{artifact_dir}{Optional directory for persisted prompts, traces,
section context, and intermediate reports.}
}
\value{
Invisibly returns a \code{pkgreviewr_report} character vector with
attached draft report, section results, section traces, artifact path, and
provenance metadata.
}
\description{
\code{build_report()} is the main end-user entry point. It collects deterministic
package diagnostics, generates report sections independently, synthesizes an
overall assessment from section summaries, optionally persists intermediate
artifacts, and writes the final markdown report to disk.
}
\details{
Supply exactly one backend source with \code{chat_fn} or \code{chat}, or configure a
local Ollama model via \code{PKGREVIEWR_OLLAMA_MODEL} or \code{OLLAMA_MODEL}.
See the \code{ellmer} package documentation for provider-specific chat object
constructors such as \code{ellmer::chat_openai()}.
}
\examples{
\dontrun{
my_chat_fn <- function(system_prompt, user_prompt) {
  stop("Connect your preferred chat backend here")
}

report <- build_report(
  "https://github.com/dvdscripter/ini",
  chat_fn = my_chat_fn,
  artifact_dir = tempfile("pkgreviewr-artifacts-")
)

chat <- ellmer::chat_openai(model = "gpt-4.1-mini")

build_report(
  "https://github.com/dvdscripter/ini",
  chat = chat,
  parallel = TRUE,
  workers = 2L
)
}
}
